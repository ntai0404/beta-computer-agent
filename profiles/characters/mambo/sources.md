# Mambo External Sources

**Canonical identity status: UNRESOLVED**

## Verified Upstream Mapping

Read-only inspection of Desktop_Gremlin `tracen` at
`feed8f6d87d8745d58c84e2a83cfa244314bdf21` found the Mambo release tag `v2.7.2`.
GitHub release metadata names it `MatikaneTannhauser v2.8` and the asset
`Mambo_v2.8.zip`. Its package configuration explicitly sets `START_CHAR = Mambo`.
The complete ZIP directory contains `SpriteSheet/Gremlins/Mambo/` and eight
`Sounds/Mambo/*.wav` entries.

This maps the alias `mambo` to an upstream package handle. It is not sufficient
to mark `Matikanetannhauser` / `Machikane Tannhauser` as a verified canonical
identity.

## Voice Evidence

| Item | Status |
|---|---|
| Upstream audio resources | Present in Desktop_Gremlin releases |
| Verified local Beta voice reference | `0` |
| Base TTS model | None found |
| RVC model | None found |
| Voice dataset | None found |
| Transcript | None found |
| External model repository URL | None referenced by inspected source/release text |
| License | Unverified; GitHub API reports no repository license |

The release EXE was not downloaded, opened, or run. Any embedded content is
therefore unresolved. Beta must not treat these WAV entries as an approved voice
sample, transcript, dataset, or character-matched output.

## Local Import Requirement

No local Tracen package was found in the approved limited discovery roots. A
user-supplied local package and explicit approval are required before any asset
inspection can produce a usable local reference. Do not copy, commit, upload,
or use upstream package assets.
